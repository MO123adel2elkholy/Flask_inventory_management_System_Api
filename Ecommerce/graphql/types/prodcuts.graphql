#  Each Category has Number of Prodcuts 
    type Product {
        id: Int!
        name: String!
        slug: String!
        description: String
        is_active: Boolean!
        category: Category
    }
